# Task

Fix the failing test described by the current issue.

## Requirements

- Inspect the failing test before editing.
- Keep the patch small.
- Add or update a regression test when the bug is real.
- Run the configured verification command before finishing.
- Summarize the changed files and verification result.
